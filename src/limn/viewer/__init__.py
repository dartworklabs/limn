"""The viewer: the page GET / serves, kept as build-free files beside this package's Python.

index.html, parts.txt, css/ and js/ are the page's source (docs/handbook/viewer.md); assemble.py puts them together
with the icons and the message table into the one HTML string server.py serves (viewer_html).
"""
